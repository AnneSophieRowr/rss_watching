= simple_form_for @feed, :html => {:class => 'form-horizontal'} do |f|

  .well
    = f.input :name

  .form_actions
    = f.button :submit, :class => 'btn btn-default'
    = link_to t('helpers.submit.cancel'), :back, :class => 'btn btn-default'

